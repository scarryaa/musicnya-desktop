import { FallbackSrc } from './fallback-src.directive';

describe('FallbackSrcDirective', () => {
  it('should create an instance', () => {
    const directive = new FallbackSrc();
    expect(directive).toBeTruthy();
  });
});
