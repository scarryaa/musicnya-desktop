import { YearSubstringPipe } from './year-slice.pipe';

describe('YearSubstringPipe', () => {
  it('create an instance', () => {
    const pipe = new YearSubstringPipe();
    expect(pipe).toBeTruthy();
  });
});
