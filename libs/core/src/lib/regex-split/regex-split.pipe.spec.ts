import { RegexSplitPipe } from './regex-split.pipe';

describe('RegexSplitPipe', () => {
  it('create an instance', () => {
    const pipe = new RegexSplitPipe();
    expect(pipe).toBeTruthy();
  });
});
