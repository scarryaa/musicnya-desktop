import { MediaHoverOverlayDirective } from './media-hover-overlay.directive';

describe('MediaHoverOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new MediaHoverOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
